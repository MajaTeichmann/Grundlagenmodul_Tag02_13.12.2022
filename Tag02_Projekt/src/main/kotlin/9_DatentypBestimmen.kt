fun main(){
    // Setze an der richtigen Stelle den passenden Datentyp für diese Variable ein
    var productCount: Int= 10

    /* Ich habe in der Zeile "var productionCount" ein ": Int" hinzugefügt, da es sich bei der Zahl 10 um eine ganze
    Zahl handelt. Wäre es eine 10.0, würde dort ein "Double" stehen müssen. */

    println("Wie lautet die Produktanzahl? $productCount")

}